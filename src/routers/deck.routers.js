const { Router } = require ('express');
const router = Router();
const deckCtrl = require('../controller/deck.controller');

// router.get('/mis-mazos', deckCtrl.getDeck);

// router.put('/mis-mazos', deckCtrl.editDeck);
