const { Router } = require ('express');
const router = Router();
const exploraCtrl = require('../controller/explora.controller');

// router.get('/explora', exploraCtrl.getDecksShared);   compartido true
// router.get('/explora/votados', exploraCtrl.getDecksVote);   compartido true
// router.get('/explora/:id_deck', exploraCtrl.getDecks);  
